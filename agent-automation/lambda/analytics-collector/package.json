{
  "name": "analytics-collector-lambda",
  "version": "1.0.0",
  "description": "Lambda function for collecting and aggregating analytics from social platforms",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "build": "zip -r function.zip src/ node_modules/",
    "deploy": "aws lambda update-function-code --function-name analytics-collector-handler --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.470.0",
    "@aws-sdk/lib-dynamodb": "^3.470.0",
    "@aws-sdk/client-secrets-manager": "^3.470.0",
    "axios": "^1.6.5",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "author": "ProDentHub Engineering",
  "license": "MIT"
}
