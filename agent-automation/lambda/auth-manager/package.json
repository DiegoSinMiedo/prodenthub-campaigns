{
  "name": "auth-manager-lambda",
  "version": "1.0.0",
  "description": "Lambda authorizer for API authentication and security",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "build": "zip -r function.zip src/ node_modules/",
    "deploy": "aws lambda update-function-code --function-name auth-manager-handler --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.470.0",
    "@aws-sdk/lib-dynamodb": "^3.470.0",
    "crypto": "^1.0.1",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "author": "ProDentHub Engineering",
  "license": "MIT"
}
